1. Python & bootstrap based web application to create, edit, import and export database data models
2. we should be able to create database connections (with jdbc, odbc). For verification, create a sqlite based database and create customer data store data model to import into data model editor
3. Keep the front end more modern and responsive
